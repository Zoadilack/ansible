---
- hosts: all
  become: true
  become_user: root
  vars:
    app_env: "dev"
  vars_files:
    - vars/all.yml
  roles:
    - role: server
    - role: composer
    - role: vagrant
    - role: nginx
    - role: mysql
    - role: phpunit
    - role: tavro

- include: playbooks/install-with-demo.yml