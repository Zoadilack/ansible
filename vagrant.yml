---
- hosts: all
  tags: vagrant
  become: true
  become_user: root
  vars_files:
    - config/vagrant.yml
    - config/parameters.yml
  roles:
    - role: server
    - role: composer
    - role: nginx
    - role: mysql
    - role: phpunit
    - role: tavro-api

- include: playbooks/install-dev.yml